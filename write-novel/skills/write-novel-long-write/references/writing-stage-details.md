# Phase 4 写作 Stage 详细说明

本文档是 `SKILL.md` Phase 4 的详细步骤展开。SKILL.md 保留阶段概览，按需加载本文件获取完整操作指令。

---

## Stage A：上下文批量加载

### A1. 细纲检查 + 标题预检

读取 `大纲/细纲_第{N}章.md`。如果不存在，**必须先补建细纲再写正文**。从细纲读取章名，如与既有章节同名或明显重复，先按本章核心事件改名，同步细纲标题。补建时参考卷纲中本章对应的事件规划和上下文。

### A2. 核心上下文加载（6 项）

**核心加载**（默认必读）：

| # | 文件 | 用途 |
|---|------|------|
| 1 | `大纲/细纲_第{N}章.md` | 本章细纲（含嵌入合约信息，frontmatter 中 cbn/cpns/cen 等字段） |
| 2 | `正文/第{N-1}章_*.md` | 上一章正文 |
| 3 | `设定/角色/{相关角色}.md` | 本章涉及角色设定（从细纲提取角色名列表后加载） |
| 4 | `追踪/状态.md` | 合并状态追踪（伏笔+时间线+角色状态+功法状态） |
| 5 | 对标书路径下 `文风.md` | 文风参考（按对标书路径查找规则） |
| 6 | 体裁画像 YAML 配置 | 体裁参数（爽点密度阈值、钩子偏好等） |

**按需加载**（仅在特定条件触发时读取）：

| 文件 | 触发条件 |
|------|---------|
| `对标/拆文报告.md` | 首次使用对标书或需要查阅套路时 |
| `对标/原文/第{N}章_*.md` | 对标书有同位置章节且需要参考时 |
| `对标/剧情/*.md` | 本章涉及对标剧情模块时 |
| `对标/设定/世界观/*.md` | 本章涉及对标世界观规则时 |
| `参考资料/*.md` | 本章涉及已研究的特殊领域时 |
| `追踪/文风缓存.md` | 非首章且同卷时（卷级缓存，命中时跳过完整文风召回） |

**快捷路径**：项目已部署 story-researcher agent 时，使用合并 query（一次 spawn 同时完成 context_load + benchmark_style_load）。agent 不可用时回退到手动分组加载。

### A3. 体裁画像加载

- 从项目设定或细纲中读取当前体裁 → 确定 profile ID
- 加载体裁画像参数（爽点密度阈值、钩子偏好、微兑现下限、节奏停滞阈值、线配比）：无对标书时使用 `references/write-gates.md` Gate 1 的默认体裁画像表；有对标书或题材特殊参数时按 `references/genre-writing-formulas.md` 对应题材参数覆盖默认值
- 注入参数到 Prewrite Gate 的爽点密度预估与线配比检查

---

## Stage B：准备与校验

### B1. 状态筛选

从已加载的上下文中提取最简记忆包（参考 state-tracking.md）——角色状态、相关伏笔/前史、世界约束。缺失时从角色设定和前文推断。

### B2. 模块召回与文风召回

① 本章目标情绪词？② 借鉴哪个参考文件的哪个技法？③ 用在哪些段落？答不出 → 先回读参考再动笔。

**(a) 文风召回（含卷级缓存）**：
- 先检查 `追踪/文风缓存.md` 是否存在且 `volume` 匹配当前卷
- 缓存命中：从 `tone_matches` 查找本章目标情绪对应的技法，直接使用
- 缓存未命中：完整执行文风召回 → 写入 `追踪/文风缓存.md`
- 文风文件不存在 → fail-fast 报错

**(b) 模块召回**：从对标的结构化子目录（角色/剧情/设定）中按本章情节检索相关模块。

**(c) Agent 快捷路径**：项目已部署 story-researcher agent 时，单次 spawn 同时完成上下文加载 + 文风召回。

### B3. 意图确认

综合细纲+最简记忆包+模块召回结果，确认本章节奏和情绪目标，用一句话概括写作意图。

### B4. Prewrite Gate 校验

详见 `references/write-gates.md` Gate 1。五项检查（前两项为新增，后三项为既有检查）：
- **frontmatter 字段完整性自检**（首项）：检查细纲 frontmatter 必需字段 `cbn`/`cpns`/`cen`/`target_words`/`strand`/`hook_type`/`payoff_density`/`event`/`conflict`/`turning_point` 齐全，缺失则从细纲正文重新提取补全，重试 2 次仍失败标记 `needs_review`，不阻塞其他章
- **细纲拆段**（新增）：将细纲的 event/conflict/turning_point 拆分为 3-6 个写作段落。每段标注：预期字数（不超过该章目标字数的 40%）、叙事功能（铺垫/推进/爆发/余韵）、对应的事件或冲突点。若 conflict.intensity >= 4 或 turning_point.is_turning = true，至少含 1 段"爆发"且预期字数不低于该章总字数的 25%。拆段结果写入细纲文件的 `## 写作段落` 段落。
- **爽点密度预估**：细纲 frontmatter `payoff_density` ≥ 适用画像（默认或题材覆盖）最低要求
- **线配比检查**：`strand` 连续同线达到适用画像上限时提示切换
- **伏笔逾期检测**：逾期伏笔优先回收

输出 prewrite 检查报告（通过/警告/阻塞），阻塞项解决前不进入 Stage C。

### B5. 资料研究（按需）

遇到需要查证的外部事实时，spawn `story-researcher` agent 输出到 `参考资料/`。

---

## Stage C：正文执行

### C1. 正文执行

第 1 章如果以内心戏、设定认知或独处开场，必须先把内心变化外化为可见事件。

**分段写作模式**（默认启用）：narrative-writer 按细纲 `## 写作段落` 逐段产出正文，段间 `---` 分隔。每段写作时只关注当前段的叙事功能（铺垫/推进/爆发/余韵），不跨越段落边界。每段开头标注叙事功能注释（如 `<!-- 铺垫 -->`）。

**连续模式**（日更默认）：不拆分段落，连续输出全文。通过 `--segmented-writing` 标志切换回分段模式。

如果项目已部署 narrative-writer agent，spawn `Agent(subagent_type: "write-novel:write-novel-narrative-writer", prompt: "项目目录：{dir}\n任务描述：写正文\n章节：第{N}章\n写作模式：{segmented/continuous}\n分段计划：{## 写作段落 内容}\n...")` 执行正文写作，输出写入 `正文/第XXX章_章名.md`。

**写作偏离处理**：允许段落数 ±1 的偏差，叙事功能可调整。偏离情况记录在文件末尾的 `<!-- deviation: ... -->` 注释中。

### C2. 字数验证

跨平台 Python 字符统计。字数 < 细纲目标的 90% → 回到细纲补充子事件，扩充正文直到达标。

---

## Stage D：质量初检（可选）

钩子检查 + 禁用词扫描：章尾是否有钩子、爽点是否到位。对照 `references/banned-words.md` 检查。

> **日更模式**：Stage D 跳过，合并到批量质量检查中执行。

---

## Stage E：Postwrite Gate（写后校验 + 追踪更新）

详见 `references/write-gates.md` Gate 2。合并原 Precommit Gate + Postcommit Gate + 追踪原子更新 + CHAPTER_COMMIT。

### 质量校验

- 字数达标（±20% 容忍）
- 合约合规（细纲 frontmatter `must_cover` 覆盖、`forbidden` 合规、CBN/CPNs/CEN 完成）
- hook 有效（非总结式结尾）
- 格式合规（段落长度、对话独立、无多余空行）
- 禁用词扫描（一级词命中即阻塞）
- 去 AI 味检查
- 投影一致性
- **段落覆盖率校验**（新增，日更模式默认跳过）：实际段落数与计划段落数偏差 ≤ 1，整体段落覆盖率 ≥ 80%（即至少 80% 的计划段落有对应正文）。未达标→生成警告写入 `追踪/状态.md` 问题列表，**不阻塞正文提交**（警告级而非阻塞级）。

### 追踪原子更新

质量校验通过后，一次性更新 `追踪/状态.md`：
- 伏笔 section：新增/回收伏笔
- 时间线 section：记录事件时序
- 角色状态 section：身份/能力/关系变化
- 功法状态 section：功法获得/升级

### CHAPTER_COMMIT

创建 `.story-system/commits/chapter_{N}.commit.md`（YAML frontmatter 记录字数/合规/审查状态）。

### 收尾

- ledger 写入：`追踪/run-ledger.md` 追加一行
- 连续线索计数：更新 strand_sequence
- 备份（可选）：复制正文到 `备份/第{N}章_*.md`

---

## Stage G：投影（精简）

每章写完后执行 2 目标投影：

1. **state**：更新 `追踪/状态.md` 的角色状态 section
2. **summary**：生成 `追踪/章节摘要/第{N}章.md`（3-5 句摘要 + 情节点列表 + 角色提及列表）

> **日更模式**：与完整模式使用相同的 2 目标投影，无差分。

---

## Stage H：安全检查

### H1. 中途快照（每连续写完 3 章执行）

- 更新 `追踪/上下文.md` 进度元信息
- 确认最近 3 个章节文件已成功写入且大小正常（>100 bytes）

> **日更模式**：Stage H 跳过（workflow-daily Step 4 已按章更新上下文.md）。

---

## Agent 调用汇总

| Stage | Agent | 条件 | 用途 |
|-------|-------|------|------|
| A2+B2 | story-researcher | agent 已部署 | 合并 context_load + benchmark_style_load |
| B5 | story-researcher | 需要查外部事实 | 资料研究，输出到 `参考资料/` |
| C1 | narrative-writer | agent 已部署 | 正文执行 |
| Phase 5 | reviewer | agent 已部署 | 一致性检查 |
| Phase 5 | narrative-writer | agent 已部署 | 去AI味审查 |

---

## 写作技巧提醒

| 场景 | 技巧 |
|------|------|
| 开篇 500 字 | 必须有钩子，不能从天气/风景开始（除非反差极大） |
| 对话 | 推进剧情或揭示性格，不能只为了凑字数 |
| 打斗 | 不要流水账，写策略和反转 |
| 日常 | 要有人物互动和伏笔 |
| 爽点释放 | 铺垫要充分、释放要干脆 |
| 章尾 | 每章结尾都要有让读者想翻下一页的东西 |

## 字数硬约束

| 节奏 | 最低字数 |
|------|----------|
| 高速推进 | ≥ 2000 字/章 |
| 正常节奏 | ≥ 3000 字/章 |
| 舒缓铺垫 | ≥ 3000 字/章 |
| 高潮爆发 | ≥ 2000 字/章 |

**默认最低字数：3000 字/章。细纲另有标注时以细纲为准。**

---

## Phase 4 新旧步骤对照表

| 新 Stage | 旧 Step | 本次优化变更 |
|----------|---------|------------|
| A1 | Step 1 + Step 6 | 细纲检查与标题预检（不变） |
| A2 | Step 2 | 13 项上下文 → 6 项核心 + 按需加载；合约信息从细纲 frontmatter 读取 |
| A3 | Step 3 | 体裁画像加载（不变） |
| B1 | Step 4.1 | 状态筛选（不变） |
| B2 | Step 4.2 | 文风召回 + 卷级缓存（不变） |
| B3 | Step 4.3 | 意图确认（不变） |
| B4 | Step 4（原 Prewrite Gate） | 简化为 3 项检查（爽点密度+线配比+伏笔逾期），合约字段从细纲 frontmatter 读取 |
| B5 | Step 5 | 资料研究（不变） |
| C1 | Step 7-8 | 正文执行（不变） |
| C2 | Step 9 | 字数验证（不变） |
| D1 | Step 10-11 | 质量初检（不变） |
| E | Step 12+14+15+16 | **合并**：Postwrite Gate（原 Precommit+Postcommit+追踪更新+CHAPTER_COMMIT+收尾） |
| G | Step 16 投影 | 精简为 2 目标（state+summary），索引/记忆降级为每 10 章 |
| H | Step 17 | 中途快照（不变） |
| — | Step 13 | 旧版无此编号 |
