"""Project diagnostics stub — runs basic checks on a novel project directory."""

def run_doctor(project_root: str) -> dict:
    return {
        "status": "ok",
        "project_root": project_root,
        "note": "stub — extend with real diagnostics as needed",
    }
