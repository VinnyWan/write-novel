## 1. 修改 Agent 调用模式

- [x] 1.1 将 Phase 1.6.3 中对 story-architect agent 的 prompt 改为要求 agent 仅返回初稿文本，不写入文件
- [x] 1.2 在主线程中增加初稿缓存变量，接收 agent 返回结果后存储全文

## 2. 世界观六大板块交互确认

- [x] 2.1 实现初稿文本解析逻辑：将 agent 返回的初稿按六大板块（时代背景、地理环境、势力格局、力量体系、社会结构）拆分
- [x] 2.2 实现逐板块展示与确认循环：每板块调用 AskUserQuestion，选项为「确认」「修改」「全部确认」
- [x] 2.3 实现「确认」处理：标记当前板块为已确认，继续下一板块
- [x] 2.4 实现「修改」处理：弹出文本输入框让用户输入修正后的完整段落，替换缓存中对应板块内容
- [x] 2.5 实现「全部确认」快捷选项：当前及后续所有未确认板块批量标记为已确认，跳出循环

## 3. 题材定位三大板块交互确认

- [x] 3.1 实现题材定位初稿解析逻辑：按三大板块（题材类型、核心梗三分法、对标作品）拆分
- [x] 3.2 复用世界观确认循环逻辑，实现题材定位逐板块确认（选项同上）

## 4. 确认后写入与 Gate 校验

- [x] 4.1 实现统一写入：所有板块确认后组装内容，写入 `设定/世界观.md` 和 `设定/题材定位.md`
- [x] 4.2 适配现有 Phase 1.6.4 Gate 校验：用户修改后如缺失 frontmatter 字段，提示补全
- [x] 4.3 确认短篇跳过逻辑不变（Phase 1.6.1 触发条件判断前置，短篇不进入交互确认）

## 5. run-ledger 记录更新

- [x] 5.1 更新 Phase 1.6 完成后的 run-ledger 记录格式，增加交互确认步数标记
